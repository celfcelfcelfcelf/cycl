import sprint from './sprint_fixture.json';
import breakaway from './breakaway_fixture.json';
import nedkorsel from './nedkorsel_fixture.json';

const fixtures = {
  sprint_fixture: sprint,
  breakaway_fixture: breakaway,
  nedkorsel_fixture: nedkorsel
};

export default fixtures;
